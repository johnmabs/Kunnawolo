import { describe,it,expect } from "vitest"; import { Identifier } from "@/shared/domain/identifier"; import { Money } from "@/shared/domain/money"; import { Quantity } from "@/shared/domain/quantity"; import { SaleLine } from "./sale-cart"; describe("SaleLine",()=>it("snapshots sale data",()=>expect(new SaleLine(Identifier.fromString("l"),Identifier.fromString("p"),"Nsiirin Ɛ",Quantity.fromNumber(1),Money.fromMinor(500,"XOF"),Money.fromMinor(300,"XOF"),Money.fromMinor(0,"XOF"))).toMatchObject({productNameSnapshot:"Nsiirin Ɛ"})));
