import "dotenv/config";
import { afterAll, beforeAll, describe, expect, it } from "vitest";
import { createPrismaClient } from "@/infrastructure/prisma/prisma-client";
import { Identifier } from "@/shared/domain/identifier";
import { CreateSaleCart } from "../application/create-sale-cart";
import { GetSaleCart } from "../application/get-sale-cart";
import { RemoveSaleLine } from "../application/remove-sale-line";
import { SaveSaleLine } from "../application/save-sale-line";
import { PrismaSaleCartRepository } from "./prisma-sale-cart-repository";
import { PrismaSalesScope } from "./prisma-sales-scope";
const url = process.env.DATABASE_URL; if (url === undefined) throw new Error("DATABASE_URL is required for sale cart integration tests.");
const prisma = createPrismaClient(url); const org = "cart-integration-org"; const otherOrg = "cart-integration-other"; const shop = "cart-integration-shop"; const product = "cart-integration-product"; let next = 0; const ids = { next: () => Identifier.fromString(`cart-integration-id-${++next}`) };
beforeAll(async () => { await prisma.organization.createMany({ data: [{ id: org, name: "Ventes", currency: "XOF" }, { id: otherOrg, name: "Autre", currency: "XOF" }], skipDuplicates: true }); await prisma.shop.upsert({ where: { id: shop }, create: { id: shop, organizationId: org, code: "VTE", name: "Boutique Ɛ" }, update: { isActive: true } }); await prisma.product.upsert({ where: { id: product }, create: { id: product, organizationId: org, name: "Nsiirin Ɛ Ɔ ɲ ŋ Fɔ́lɔ" }, update: { isActive: true } }); await prisma.productPrice.upsert({ where: { id: "cart-integration-price" }, create: { id: "cart-integration-price", organizationId: org, productId: product, referenceCostMinor: 500, salePriceMinor: 800, currency: "XOF", reference: "Tarif Ɛ", actorId: "actor" }, update: {} }); });
afterAll(async () => { await prisma.saleLine.deleteMany({ where: { saleCart: { organizationId: org } } }); await prisma.saleCart.deleteMany({ where: { organizationId: org } }); await prisma.productPrice.deleteMany({ where: { id: "cart-integration-price" } }); await prisma.product.deleteMany({ where: { id: product } }); await prisma.shop.deleteMany({ where: { id: shop } }); await prisma.organizationAudit.deleteMany({ where: { organizationId: { in: [org, otherOrg] } } }); await prisma.organization.deleteMany({ where: { id: { in: [org, otherOrg] } } }); await prisma.$disconnect(); });
describe("PrismaSaleCartRepository", () => { it("persists the complete Unicode snapshot lifecycle and keeps historical carts readable", async () => { const carts = new PrismaSaleCartRepository(prisma); const scope = new PrismaSalesScope(prisma); const cart = await new CreateSaleCart(scope, carts, ids).execute({ organizationId: org, shopId: shop, actorId: "actor-1" }); const save = new SaveSaleLine(scope, carts, ids); const line = await save.execute({ organizationId: org, cartId: cart.id.value, productId: product, quantity: 2, discountMinor: 100, actorId: "actor-2" }); await save.execute({ organizationId: org, cartId: cart.id.value, lineId: line.id.value, productId: product, quantity: 3, discountMinor: 0, actorId: "actor-3" }); await prisma.shop.update({ where: { id: shop }, data: { isActive: false } }); await expect(new GetSaleCart(carts).execute({ organizationId: org, cartId: cart.id.value })).resolves.toMatchObject({ lines: [{ productNameSnapshot: "Nsiirin Ɛ Ɔ ɲ ŋ Fɔ́lɔ", quantity: { value: 3 }, unitPrice: { amountMinor: 800 }, unitCost: { amountMinor: 500 } }] }); await expect(new GetSaleCart(carts).execute({ organizationId: otherOrg, cartId: cart.id.value })).rejects.toMatchObject({ code: "sales.cart_not_found" }); await new RemoveSaleLine(carts).execute({ organizationId: org, cartId: cart.id.value, lineId: line.id.value, actorId: "actor-4" }); await expect(prisma.saleLine.count({ where: { saleCartId: cart.id.value } })).resolves.toBe(0); await expect(prisma.organizationAudit.count({ where: { organizationId: org, action: { in: ["sale_cart.created", "sale_line.added", "sale_line.updated", "sale_line.removed"] } } })).resolves.toBe(4); }); });
