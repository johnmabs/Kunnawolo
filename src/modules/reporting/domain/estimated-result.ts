import { DomainError } from "@/shared/domain/domain-error";
import { Identifier } from "@/shared/domain/identifier";
import { Money } from "@/shared/domain/money";
export class EstimatedResult { private constructor(public readonly organizationId: Identifier, public readonly shopId: Identifier | null, public readonly grossMargin: Money, public readonly activeExpenses: Money, public readonly valuedLosses: Money, public readonly amount: Money) {} public static create(input: Readonly<{ organizationId: string; shopId: string | null; grossMargin: Money; activeExpenses: Money; valuedLosses: Money }>) { if (input.grossMargin.currency !== input.activeExpenses.currency || input.grossMargin.currency !== input.valuedLosses.currency) throw new DomainError("reporting.currency_mismatch", "Estimated result inputs must share a currency."); return new EstimatedResult(Identifier.fromString(input.organizationId), input.shopId === null ? null : Identifier.fromString(input.shopId), input.grossMargin, input.activeExpenses, input.valuedLosses, Money.fromMinor(input.grossMargin.amountMinor - input.activeExpenses.amountMinor - input.valuedLosses.amountMinor, input.grossMargin.currency)); } }
