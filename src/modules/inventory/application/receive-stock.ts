import { DomainError } from "@/shared/domain/domain-error";
import { RecordStockMovement } from "./record-stock-movement";
export class ReceiveStock { public constructor(private readonly record: RecordStockMovement) {} public async execute(input: Readonly<{ organizationId: string; shopId: string; productId: string; quantity: number; reference: string; actorId: string | null; idempotencyKey: string }>) { if (!Number.isFinite(input.quantity) || input.quantity <= 0) throw new DomainError("inventory.invalid_entry_quantity", "A stock entry quantity must be strictly positive."); return this.record.execute({ organizationId: input.organizationId, shopId: input.shopId, productId: input.productId, quantityDelta: input.quantity, reason: `stock.entry:${input.reference}`, actorId: input.actorId, idempotencyKey: input.idempotencyKey }); } }
