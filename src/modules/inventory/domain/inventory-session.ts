import { DomainError } from "@/shared/domain/domain-error";
import { Identifier } from "@/shared/domain/identifier";
import { Quantity } from "@/shared/domain/quantity";
export class InventorySession { private constructor(public readonly id: Identifier, public readonly organizationId: Identifier, public readonly shopId: Identifier, public readonly lines: readonly InventorySessionLine[] = []) {} public static open(id: Identifier, organizationId: Identifier, shopId: Identifier, lines: readonly InventorySessionLine[]): InventorySession { return new InventorySession(id, organizationId, shopId, lines); } public close(): void { if (this.lines.some((line) => line.countedQuantity === null)) throw new DomainError("inventory.unfinished_count", "Every inventory line must be counted before closing."); } }
export class InventorySessionLine { private constructor(public readonly id: Identifier, public readonly productId: Identifier, public readonly expectedQuantity: Quantity, public readonly countedQuantity: Quantity | null) {} public static snapshot(id: Identifier, productId: Identifier, expectedQuantity: Quantity, countedQuantity: Quantity | null = null): InventorySessionLine { return new InventorySessionLine(id, productId, expectedQuantity, countedQuantity); } public count(quantity: Quantity): InventorySessionLine { return InventorySessionLine.snapshot(this.id, this.productId, this.expectedQuantity, quantity); } }
