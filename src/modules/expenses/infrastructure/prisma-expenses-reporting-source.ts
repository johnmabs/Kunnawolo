import type { PrismaClient } from "@/infrastructure/prisma/generated/client";
import { DomainError } from "@/shared/domain/domain-error";
import type { ExpensesReportingSource } from "../application/ports/expenses-reporting-source";
export class PrismaExpensesReportingSource implements ExpensesReportingSource { public constructor(private readonly prisma: PrismaClient) {} public async activeExpenseAmount(input: Readonly<{ organizationId: string; shopId: string | null; occurredFrom: Date | null; occurredTo: Date | null }>) { const org = await this.prisma.organization.findUnique({ where: { id: input.organizationId }, select: { currency: true } }); if (org === null) throw new DomainError("reporting.organization_not_found", "The organization does not exist."); const rows = await this.prisma.expense.findMany({ where: { organizationId: input.organizationId, ...(input.shopId === null ? {} : { shopId: input.shopId }), cancellation: null, ...(input.occurredFrom === null && input.occurredTo === null ? {} : { occurredAt: { ...(input.occurredFrom === null ? {} : { gte: input.occurredFrom }), ...(input.occurredTo === null ? {} : { lte: input.occurredTo }) } }) }, select: { amountMinor: true, currency: true } }); let amountMinor = 0; for (const row of rows) { if (row.currency !== org.currency) throw new DomainError("reporting.invalid_expense_snapshot", "An expense currency does not match its organization."); amountMinor += Number(row.amountMinor); if (!Number.isSafeInteger(amountMinor)) throw new DomainError("reporting.estimated_result_overflow", "The estimated result exceeds safe money bounds."); } return { currency: org.currency, amountMinor }; } }
